Description: Revert notify patch, prepare for fly-notification
Author: Gennady Vasilev <gvailev@astralinux.ru>
Last-Update: 2024-09-04

--- strongswan-5.9.8.orig/src/libstrongswan/plugins/pkcs11/pkcs11_private_key.c
+++ strongswan-5.9.8/src/libstrongswan/plugins/pkcs11/pkcs11_private_key.c
@@ -44,8 +44,6 @@
 
 #include <utils/debug.h>
 #include <asn1/asn1.h>
-#include <stdio.h>
-#include <stdlib.h>
 
 typedef struct private_pkcs11_private_key_t private_pkcs11_private_key_t;
 
@@ -775,24 +773,16 @@ static bool login(private_pkcs11_private
 			success = TRUE;
 			break;
 		}
-		DBG1(DBG_CFG, "login to '%s':%d failed: %N",
-			 this->lib->get_name(this->lib), slot, ck_rv_names, rv);
-		char* sudo_user = getenv("SUDO_USER");
-		if (sudo_user) {
-			char cmd[1024];
-			memset(cmd, 0, sizeof(cmd));
-			if (rv == CKR_PIN_INCORRECT)
-				sprintf(cmd, "sudo -u %s DISPLAY=:0 notify-send \"%s\" \"%s\"", sudo_user, "strongswan: ошибка токена", "Неверный пин-код");
-			if (rv == CKR_PIN_INVALID)
-				sprintf(cmd, "sudo -u %s DISPLAY=:0 notify-send \"%s\" \"%s\"", sudo_user, "strongswan: ошибка токена", "Пин-код содержит недопустимые символы");
-			if (rv == CKR_PIN_LEN_RANGE)
-				sprintf(cmd, "sudo -u %s DISPLAY=:0 notify-send \"%s\" \"%s\"", sudo_user, "strongswan: ошибка токена", "Неверная длина пин-кода");
-			if (rv == CKR_PIN_EXPIRED)
-				sprintf(cmd, "sudo -u %s DISPLAY=:0 notify-send \"%s\" \"%s\"", sudo_user, "strongswan: ошибка токена", "Истек срок действия пин-кода");
-			if (rv == CKR_PIN_LOCKED)
-				sprintf(cmd, "sudo -u %s DISPLAY=:0 notify-send \"%s\" \"%s\"", sudo_user, "strongswan: ошибка токена", "Токен заблокирован");
-			system(cmd);
-		}
+		if (rv == CKR_PIN_INCORRECT)
+			DBG1(DBG_CFG, "strongswan: ошибка токена (%s)", "Неверный пин-код");
+		if (rv == CKR_PIN_INVALID)
+			DBG1(DBG_CFG, "strongswan: ошибка токена (%s)", "Пин-код содержит недопустимые символы");
+		if (rv == CKR_PIN_LEN_RANGE)
+			DBG1(DBG_CFG, "strongswan: ошибка токена (%s)", "Неверная длина пин-кода");
+		if (rv == CKR_PIN_EXPIRED)
+			DBG1(DBG_CFG, "strongswan: ошибка токена (%s)", "Истек срок действия пин-кода");
+		if (rv == CKR_PIN_LOCKED)
+			DBG1(DBG_CFG, "strongswan: ошибка токена (%s)", "Токен заблокирован");
 	}
 	enumerator->destroy(enumerator);
 
